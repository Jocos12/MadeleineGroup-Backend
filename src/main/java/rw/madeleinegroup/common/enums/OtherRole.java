package rw.madeleinegroup.common.enums;

/**
 * Roles for users with role=OTHER.
 * CEO defines the specific role when creating a user.
 */
public enum OtherRole {
    CLEAN,
    DJ,
    CHEF,
    PHOTOGRAPHER,
    DECORATOR
}
