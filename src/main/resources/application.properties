server.port=8082

spring.datasource.url=jdbc:mysql://localhost:3308/madleine_group?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=${DB_PASSWORD:Prince Jocos9}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
file.upload-dir=./uploads
app.upload.dir=uploads/profile-photos
app.api-base-url=http://localhost:8082

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=universitevirtuelledutchad@gmail.com
spring.mail.password=tvfdjpmxdzsvqbjl
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
# Trust SSL certs (* = trust all, for dev when PKIX errors occur)
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000

# JWT
jwt.secret=${JWT_SECRET:MadeleineGroupSecretKeyForJWTTokenGeneration2024SecureKey}
jwt.expiration=86400000

# OTP (log-to-console: when true, OTP is printed in server logs if email fails - useful for dev)
app.otp.log-to-console=true
otp.expiration-minutes=10
otp.max-attempts=5

# AI Advisor (Anthropic)
app.anthropic.api-key=${ANTHROPIC_API_KEY:}

# Groq AI (financial advisor ? key in application-local.properties or env to avoid committing)

groq.api.url=https://api.groq.com/openai/v1/chat/completions
groq.model=llama-3.3-70b-versatile
groq.max.tokens=1500
groq.temperature=0.7

# CORS (frontend URL)
app.cors.allowed-origins=http://localhost:3000

# Logo URL for reminder emails (public URL; imgbb format). Used in email img src.
app.logo.url=https://i.ibb.co/8LsNW25Z/madeleine-Group-Logo.png
# Contact info shown in reminder email footer
app.contact.phone=+250 788 123 456
app.contact.email=contact@madeleinegroup.rw

# MVC: avoid NoResourceFoundException being thrown when API endpoints don't match (return 404 instead of 500)
spring.mvc.throw-exception-if-no-handler-found=false

# Logging
logging.level.org.springframework.transaction=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE